import 'package:flutter/material.dart';

/// Minimal stub to represent premium download manager screen
class PremiumDownloadManager extends StatelessWidget {
  const PremiumDownloadManager({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Premium Download Manager (stub)', style: Theme.of(context).textTheme.titleLarge),
    );
  }
}
